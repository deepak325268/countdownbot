name: Daily Bot Trigger

on:
  schedule:
    - cron: "0 4 * * *" # हर दिन सुबह 9:30 AM IST (UTC: 4:00 AM)
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Railway Bot
        run: curl -s https://YOUR_RAILWAY_URL
